# 2.2 Web数据库应用系统体系结构

+ 客户端层（Browser/Client）：用户通过浏览器或应用界面发送请求，显示结果
+ Web服务器层（Web Server）：处理 HTTP 请求，进行页面渲染、请求转发
+ 应用逻辑层（Application Server）：实现业务逻辑、数据处理、事务管理
+ 数据库层（Database Server）：存储和管理数据，提供数据查询和更新接口

常见组织形式：客户端 ↔ 应用逻辑层 ↔ 数据库
