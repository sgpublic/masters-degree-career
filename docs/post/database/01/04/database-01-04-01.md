# 1.4.1 MySQL源代码编译

编译关键步骤如下：

1. 为系统安装关键依赖，例如 cmake 等。
2. 下载源码并解压，或从 MySQL 官方仓库克隆源码。
3. 创建编译目录，然后使用 `cmake -S <源码目录> -B <编译目录>` 创建编译目标。
4. 进入编译目录，使用 `make -C <编译目录>` 开始编译。
5. 编译完成后，使用 `make install -C <编译目录>` 安装编译产物。
